export enum DataTablesColumnType {
    STRING = 'STRING',
    INT = 'INT',
    DECIMAL = 'DECIMAL',
    BOOLEAN = 'BOOLEAN',
    SELECT = 'SELECT',
    CURRENCY = 'CURRENCY',
    ACTION = 'ACTION',
    DATE = 'DATE',
}
