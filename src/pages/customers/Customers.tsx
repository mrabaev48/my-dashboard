import React from 'react';
import Typography from "@material-ui/core/Typography";

export const Customers: React.FC = () => {
    return (
        <Typography variant="h3" component="h1">Customers Page</Typography>
    );
};