import React from 'react';
import Typography from "@material-ui/core/Typography";

export const Reports: React.FC = () => {
    return (
        <Typography variant="h3" component="h1">Reports Page</Typography>
    );
};