import React from 'react';
import Typography from "@material-ui/core/Typography";

export const Dashboard: React.FC = () => {
    return (
        <Typography variant="h4" component="h1">Dashboard Page</Typography>
    );
};